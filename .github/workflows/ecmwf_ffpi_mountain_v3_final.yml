name: ECMWF FFPI Mountain V3 Final Test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  mountain-v3-final-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install numpy pandas xarray cfgrib eccodes matplotlib cartopy scipy ecmwf-opendata

      - name: Run ECMWF FFPI Mountain V3 Final
        run: python ecmwf_ffpi_mountain_v3_final.py

      - name: Save V3 Final map and diagnostics
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add output/
          git diff --cached --quiet || git commit -m "Update ECMWF FFPI Mountain V3 Final test"
          git push
